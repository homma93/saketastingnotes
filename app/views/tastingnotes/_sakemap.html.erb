<div class="col-sm-6 border">
  <div class="text-left mb-4 mt-4">
    <h4>My日本酒マップ</h4>
  </div>
  <div>
    <%= image_tag 'map.png', class: 'img-fluid border rounded' %>
    <p>100</p>
  </div>
  <div class="row mt-4">
    <div class="col-sm-12 h5">
      総テイスティング数：<%= @tastingnote_nums %>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-12">
      <table class="table table-striped table-bordered">
        <tr>
          <th class="text-center">地域</th>
          <th class="text-center">テイスティング数</th>
          <th class="text-center">テイスティング割合</th>
          <th class="text-center">酒蔵数</th>
          <th class="text-center">酒蔵制覇割合</th>
        </tr>
        <% tastingnote_table(@tastingnote_nums, @tastingnote_region_nums, @sakagura_todofuken_nums, @tastingnote_sakagura_nums) %>
        <% @tastingnote_row.each do |a, b, c, d, e| %>
          <tr>
            <td class="text-center"><%= a %></td>
            <td class="text-center"><%= b %>回</td>
            <td class="text-center"><%= c %>%</td>
            <td class="text-center"><%= d %></td>
            <td class="text-center"><%= e %>%</td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
  <div class="row mt-4">
    <div class="col-sm-4">
      地元の日本酒を探そう！
    </div>
    <div class="col-sm-8">
      まだ見ぬ日本酒を探そう！
    </div>
  </div>
  <div class="row mb-4">
    <div class="col-sm-4">
      <%= link_to place(@region_id.first.todofuken.region_id.to_i), todofukens_path(serch: 2, region_id:8), class: 'btn btn-outline-info btm-xs btn-block' %>
    </div>
    <% random_place(@region_id.first.todofuken.region_id.to_i) %>
    <div class="col-sm-4">
      <%= link_to place(@place[0]), todofukens_path(serch: 2, region_id:@place[0]), class: 'btn btn-outline-info btm-xs btn-block' %>
    </div>
    <div class="col-sm-4">
      <%= link_to place(@place[1]), todofukens_path(serch: 2, region_id:@place[1]), class: 'btn btn-outline-info btm-xs btn-block' %>
    </div>
  </div>
</div>